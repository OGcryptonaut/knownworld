# Real Telegram exports and any local corpus — MUST never be committable
data-local/
**/data-local/
result.json
**/result.json
*.telegram-export*
ChatExport*/
# ...except the FICTIONAL generated sample (sample-data/generate.py, seed 42)
!sample-data/result.json

# Local databases / sessions
*.sqlite
*.sqlite3
*.db
*.session

# Secrets
.env
.env.*
!.env.example
*service-account*.json
*credentials*.json

# Node / Next / Python
node_modules/
.next/
out/
dist/
build/
coverage/
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
.idea/
*.swp

# Local artifacts
.firebase/
.gcloudignore.local
tmp/

# v2 local persistence (STORE_MODE=local) — per-user distilled data, never committable
data-local-store/
**/data-local-store/
