# Knownworld — Kickoff for Claude Code (hackathon build)

Unzip knownworld-hackathon-pack.zip at ~/Knownworld first. Then this is message 1.

1. Work ONLY in ~/Knownworld. Never read from or write to ~/MojoOS. Proven
   MojoOS code (whole-token company matcher, etc.) arrives only when I paste
   it explicitly.
2. Read, in order: SPEC-HACKATHON.md (governs), CLAUDE.md, ROADMAP-HACKATHON.md,
   SPEC.md (product flow v1+v1.1), docs/ (each file's "Hackathon variant"
   section governs), data/ats-slugs.json.
3. git init. PRIVATE GitHub repo `knownworld` via gh. Commit 1 contains the
   .gitignore: data-local/, *.sqlite, *.session, .env*, any Telegram export.
   My real export is the test corpus and must never be committable. Before
   submission we share the repo with testing@devpost.com and
   cloudhackathons@google.com.
4. Deadline: Aug 31 5pm PDT (Sep 1 01:00 Lisbon). Every commit inside the
   window — history is compliance proof. The skeleton docs are disclosed
   prior ideation; all code starts now.
5. I have a Google Cloud trial + $150 credits and will paste project ID and
   any keys when you ask. Gemini via API key or Vertex — your call, justify.
6. Product name is Knownworld ("this is my known world"). Elevator: turns an
   individual's own Telegram DMs into a personal warm-network contact
   database for personal networking + job discovery.
7. Reply with: (a) setup done, (b) your D0 plan per ROADMAP-HACKATHON.md,
   (c) exactly what you need from me — then WAIT for my GO. One GO per day,
   evidence-gated report per day, same standard as MojoOS ships.
